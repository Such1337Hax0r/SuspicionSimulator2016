using System.IO;
using UnityEngine;
using System.Collections;
public class codenstuff
  {
    public static void main() {
      Console.WriteLine("Initializing");
      FileStream F = new FileStream("Characters.dat", FileMode.Open, FileAccess.ReadWrite)
      String dataNStuff[] = new String[];
      for (int i = 0; i < )
    }
  }
